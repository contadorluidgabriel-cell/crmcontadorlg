# CRM Comercial — Contador Luid Gabriel

## Baseline V2.1

A branch `main` marca o ponto estável anterior à refatoração para React/JSX.

A nova arquitetura está sendo desenvolvida de forma isolada em `v2.2-react` e só deve ser integrada à `main` depois da validação visual e funcional.

### Estado atual

- Persistência da fase V2.1: localStorage.
- V2.2: React/JSX, Contatos + Empresas + Oportunidades e propostas versionadas.
- Pull request da V2.2 deve permanecer em rascunho até validação.

O snapshot completo da V2.1 continua preservado no bundle/arquivo de desenvolvimento gerado antes da migração; ele não foi duplicado nesta branch durante o bootstrap inicial do repositório.
